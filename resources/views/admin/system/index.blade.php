@extends('layouts.portal')

@section('title', 'System Administration')

@section('content')
    <header class="page-heading">
        <p class="eyebrow">Operational administration</p>
        <h1>System Health and Settings</h1>
        <p>Inspect database, storage, scheduler, queues, failed jobs, audit activity and mail delivery without exposing application secrets.</p>
    </header>

    <section class="system-overall-status system-status-{{ $health['overall'] }}">
        <div><span>Overall system status</span><strong>{{ str($health['overall'])->headline() }}</strong></div>
        <div><span>Checked</span><strong>{{ $health['checked_at']->format('d M Y H:i:s') }}</strong></div>
        <div><span>Environment</span><strong>{{ str($health['environment'])->headline() }}</strong></div>
        <div><span>Debug</span><strong>{{ $health['app_debug'] ? 'Enabled' : 'Disabled' }}</strong></div>
    </section>

    <nav class="workspace-tabs" aria-label="System administration sections">
        @foreach (['health' => 'Health', 'audit' => 'Audit Logs', 'jobs' => 'Queues and Jobs', 'mail' => 'Mail and SMTP', 'settings' => 'Operations Settings'] as $key => $label)
            <a href="{{ route('web.admin.system.index', ['section' => $key]) }}" @class(['is-active' => $activeSection === $key])>{{ $label }}</a>
        @endforeach
    </nav>

    @if ($activeSection === 'health')
        <section class="health-check-list">
            @foreach ($health['checks'] as $check)
                <article class="health-check-row health-check-{{ $check['status'] }}"><div><span>{{ $check['label'] }}</span><strong>{{ str($check['status'])->headline() }}</strong></div><p>{{ $check['message'] }}</p><dl>@foreach(collect($check)->except(['label','status','message']) as $key => $value)<div><dt>{{ str($key)->headline() }}</dt><dd>{{ $value instanceof \Carbon\CarbonInterface ? $value->format('d M Y H:i:s') : (is_bool($value) ? ($value ? 'Yes' : 'No') : ($value ?? '—')) }}</dd></div>@endforeach</dl></article>
            @endforeach
        </section>
        <section class="content-section"><div class="section-heading"><div><p class="eyebrow">Runtime</p><h2>Application versions</h2></div></div><dl class="system-fact-list"><div><dt>Laravel</dt><dd>{{ $health['laravel_version'] }}</dd></div><div><dt>PHP</dt><dd>{{ $health['php_version'] }}</dd></div><div><dt>Queue connection</dt><dd>{{ config('queue.default') }}</dd></div><div><dt>Application timezone</dt><dd>{{ config('app.timezone') }}</dd></div></dl></section>
    @elseif ($activeSection === 'audit')
        <form method="GET" action="{{ route('web.admin.system.index') }}" class="filter-bar system-audit-filters"><input type="hidden" name="section" value="audit"><label class="field-group"><span>User</span><select name="audit_user_id"><option value="">All users</option>@foreach($auditUsers as $user)<option value="{{ $user->id }}" @selected((string) request('audit_user_id') === (string) $user->id)>{{ $user->fullName() }}</option>@endforeach</select></label><label class="field-group"><span>Method</span><select name="audit_method"><option value="">All methods</option>@foreach(['GET','POST','PUT','PATCH','DELETE'] as $method)<option value="{{ $method }}" @selected(request('audit_method') === $method)>{{ $method }}</option>@endforeach</select></label><label class="field-group"><span>Action</span><input name="audit_action" value="{{ request('audit_action') }}"></label><label class="field-group"><span>Route</span><input name="audit_route" value="{{ request('audit_route') }}"></label><label class="field-group"><span>Status</span><input name="audit_status" type="number" value="{{ request('audit_status') }}"></label><label class="field-group"><span>From</span><input name="audit_from" type="date" value="{{ request('audit_from') }}"></label><label class="field-group"><span>To</span><input name="audit_to" type="date" value="{{ request('audit_to') }}"></label><button class="secondary-button" type="submit">Apply filters</button></form>
        <div class="data-table-wrap"><table class="data-table"><thead><tr><th>Time</th><th>User</th><th>Request</th><th>Route/action</th><th>Status</th><th>IP address</th><th>Metadata</th></tr></thead><tbody>@forelse($auditLogs as $log)<tr><td>{{ $log->created_at->format('d M Y H:i:s') }}</td><td>{{ $log->user?->fullName() ?? 'Guest/System' }}<small>{{ $log->user?->roleLabel() }}</small></td><td><strong>{{ $log->method }}</strong><small>{{ str($log->path)->limit(90) }}</small></td><td>{{ $log->route ?: 'Unnamed route' }}<small>{{ $log->action }}</small></td><td><span class="status-badge status-{{ ($log->status_code ?? 500) < 400 ? 'active' : 'inactive' }}">{{ $log->status_code ?: '—' }}</span></td><td>{{ $log->ip_address ?: '—' }}</td><td><details><summary>View</summary><pre>{{ json_encode($log->metadata, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) }}</pre></details></td></tr>@empty<tr><td colspan="7">No audit events match these filters.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $auditLogs->links() }}</div>
    @elseif ($activeSection === 'jobs')
        <section class="queue-summary-grid"><article><span>Pending jobs</span><strong>{{ number_format($pendingJobs->count()) }}</strong><small>First 100 shown</small></article><article><span>Failed jobs</span><strong>{{ number_format($failedJobs->count()) }}</strong><small>First 100 shown</small></article><article><span>Queue connection</span><strong>{{ config('queue.default') }}</strong><small>Worker must run continuously</small></article></section>
        <section class="content-section"><div class="section-heading"><div><p class="eyebrow">Pending</p><h2>Queued work</h2></div></div><div class="data-table-wrap"><table class="data-table"><thead><tr><th>ID</th><th>Queue</th><th>Attempts</th><th>Available</th><th>Created</th></tr></thead><tbody>@forelse($pendingJobs as $job)<tr><td>{{ $job->id }}</td><td>{{ $job->queue }}</td><td>{{ $job->attempts }}</td><td>{{ \Carbon\Carbon::createFromTimestamp($job->available_at)->format('d M Y H:i:s') }}</td><td>{{ \Carbon\Carbon::createFromTimestamp($job->created_at)->format('d M Y H:i:s') }}</td></tr>@empty<tr><td colspan="5">No pending jobs.</td></tr>@endforelse</tbody></table></div></section>
        <section class="content-section"><div class="section-heading"><div><p class="eyebrow">Failures</p><h2>Failed jobs</h2></div><p>Retry only after correcting the underlying configuration or data problem.</p></div><div class="failed-job-list">@forelse($failedJobs as $job)<details><summary><span><strong>{{ $job->uuid }}</strong><small>{{ $job->connection }} · {{ $job->queue }} · {{ \Carbon\Carbon::parse($job->failed_at)->format('d M Y H:i:s') }}</small></span><span aria-hidden="true">+</span></summary><pre>{{ str($job->exception)->limit(5000) }}</pre><div class="inline-actions"><form method="POST" action="{{ route('web.admin.system.failed-jobs.retry', $job->uuid) }}">@csrf<button class="primary-button" type="submit">Retry job</button></form><form method="POST" action="{{ route('web.admin.system.failed-jobs.destroy', $job->uuid) }}">@csrf @method('DELETE')<button class="secondary-button" type="submit">Delete failure record</button></form></div></details>@empty<div class="empty-state">No failed jobs.</div>@endforelse</div></section>
    @elseif ($activeSection === 'mail')
        <div class="split-workspace"><section class="workspace-primary"><div class="section-heading compact-heading"><div><p class="eyebrow">Mail transport</p><h2>SMTP configuration</h2></div><p>Passwords are encrypted at rest. Leaving the password blank preserves the existing encrypted value.</p></div><form method="POST" action="{{ route('web.admin.system.mail.update') }}" class="long-form">@csrf @method('PUT')<fieldset class="form-section"><legend>Transport</legend><div class="form-grid form-grid-3"><label class="field-group"><span>Mailer</span><select name="mail_mailer"><option value="smtp" @selected(($settings['mail_mailer'] ?? config('mail.default')) === 'smtp')>SMTP</option><option value="log" @selected(($settings['mail_mailer'] ?? config('mail.default')) === 'log')>Log only</option><option value="array" @selected(($settings['mail_mailer'] ?? config('mail.default')) === 'array')>In-memory testing</option></select></label><label class="field-group"><span>Host</span><input name="mail_host" value="{{ old('mail_host', $settings['mail_host'] ?? '') }}"></label><label class="field-group"><span>Port</span><input name="mail_port" type="number" min="1" max="65535" value="{{ old('mail_port', $settings['mail_port'] ?? 587) }}"></label><label class="field-group"><span>Scheme</span><select name="mail_scheme"><option value="">Automatic</option><option value="smtp" @selected(($settings['mail_scheme'] ?? '') === 'smtp')>SMTP</option><option value="smtps" @selected(($settings['mail_scheme'] ?? '') === 'smtps')>SMTPS</option></select></label><label class="field-group"><span>Username</span><input name="mail_username" value="{{ old('mail_username', $settings['mail_username'] ?? '') }}"></label><label class="field-group"><span>Password</span><input name="mail_password" type="password" autocomplete="new-password" placeholder="Leave blank to preserve"></label><label class="field-group"><span>Timeout seconds</span><input name="mail_timeout" type="number" min="5" max="120" value="{{ old('mail_timeout', $settings['mail_timeout'] ?? 20) }}"></label></div></fieldset><fieldset class="form-section"><legend>Sender identity</legend><div class="form-grid form-grid-2"><label class="field-group"><span>From address</span><input name="mail_from_address" type="email" value="{{ old('mail_from_address', $settings['mail_from_address'] ?? config('mail.from.address')) }}" required></label><label class="field-group"><span>From name</span><input name="mail_from_name" value="{{ old('mail_from_name', $settings['mail_from_name'] ?? config('app.name')) }}" required></label></div></fieldset><div class="form-actions"><button class="primary-button" type="submit">Save encrypted mail settings</button></div></form></section><aside class="workspace-aside"><section class="plain-panel"><div class="section-heading compact-heading"><div><p class="eyebrow">Current status</p><h2>Test delivery</h2></div></div><dl class="system-fact-list"><div><dt>Mailer</dt><dd>{{ $mailStatus['mailer'] }}</dd></div><div><dt>SMTP fields</dt><dd>{{ $mailStatus['smtp_configured'] ? 'Present' : 'Incomplete' }}</dd></div><div><dt>From</dt><dd>{{ $mailStatus['from_address'] }}</dd></div></dl><form method="POST" action="{{ route('web.admin.system.mail.test') }}" class="form-stack">@csrf<label class="field-group"><span>Test recipient</span><input name="test_email" type="email" value="{{ auth()->user()->email }}" required></label><button class="secondary-button" type="submit">Send synchronous test</button></form></section></aside></div>
    @elseif ($activeSection === 'settings')
        <section class="content-section"><div class="section-heading"><div><p class="eyebrow">Operations</p><h2>Monitoring thresholds and retention</h2></div></div><form method="POST" action="{{ route('web.admin.system.settings.update') }}" class="long-form">@csrf @method('PUT')<div class="form-grid form-grid-2"><label class="field-group"><span>Maintenance contact email</span><input name="maintenance_contact" type="email" value="{{ old('maintenance_contact', $settings['maintenance_contact'] ?? '') }}"></label><label class="field-group"><span>Operations timezone</span><input name="operations_timezone" value="{{ old('operations_timezone', $settings['operations_timezone'] ?? config('app.timezone')) }}" required></label><label class="field-group"><span>Queue warning minutes</span><input name="queue_warning_minutes" type="number" min="5" max="1440" value="{{ old('queue_warning_minutes', $settings['queue_warning_minutes'] ?? 15) }}" required></label><label class="field-group"><span>Scheduler warning minutes</span><input name="scheduler_warning_minutes" type="number" min="2" max="60" value="{{ old('scheduler_warning_minutes', $settings['scheduler_warning_minutes'] ?? 5) }}" required></label><label class="field-group"><span>Audit retention days</span><input name="audit_retention_days" type="number" min="30" max="3650" value="{{ old('audit_retention_days', $settings['audit_retention_days'] ?? 365) }}" required></label></div><div class="form-actions"><button class="primary-button" type="submit">Save operations settings</button></div></form></section>
    @endif
@endsection
