@extends('layouts.portal')

@section('title', 'Website CMS')

@section('content')
    <header class="page-heading page-heading-actions">
        <div>
            <p class="eyebrow">Public website administration</p>
            <h1>Website CMS</h1>
            <p>Manage public pages, hero and gallery media, school news, testimonials, enquiries, subscribers and school contact information.</p>
        </div>
        <div class="inline-actions">
            @if (auth()->user()->hasPermission('website.manage_themes'))<a class="secondary-link" href="{{ route('web.admin.website.themes.index') }}">Theme Manager</a>@endif
            <a class="primary-link" href="{{ route('public.home') }}" target="_blank">Open Public Website</a>
        </div>
    </header>

    <section class="metric-row metric-row-5" aria-label="Website summary">
        <article class="metric-item"><span>Published pages</span><strong>{{ $counts['published_pages'] }}</strong></article>
        <article class="metric-item"><span>Published news</span><strong>{{ $counts['published_news'] }}</strong></article>
        <article class="metric-item"><span>Public media</span><strong>{{ $counts['public_media'] }}</strong></article>
        <article class="metric-item"><span>New enquiries</span><strong>{{ $counts['new_messages'] }}</strong></article>
        <article class="metric-item"><span>Subscribers</span><strong>{{ $counts['active_subscribers'] }}</strong></article>
    </section>

    <nav class="workspace-tabs" aria-label="Website CMS sections">
        @foreach ([
            'overview' => 'Overview',
            'pages' => 'Pages',
            'media' => 'Media',
            'news' => 'News',
            'testimonials' => 'Testimonials',
            'messages' => 'Enquiries',
            'newsletter' => 'Newsletter',
            'settings' => 'School Details',
        ] as $key => $label)
            <a href="{{ route('web.admin.website.index', ['section' => $key]) }}" @class(['is-active' => $activeSection === $key])>{{ $label }}</a>
        @endforeach
    </nav>

    @if ($activeSection === 'overview')
        <section class="cms-overview-list">
            <article><span>01</span><div><h2>Public pages</h2><p>Edit homepage, About, Admissions and Contact content with SEO metadata and publication controls.</p></div><a href="{{ route('web.admin.website.index', ['section' => 'pages']) }}">Manage pages</a></article>
            <article><span>02</span><div><h2>Public media</h2><p>Upload or link hero images, campus videos and gallery media with captions and accessible alternative text.</p></div><a href="{{ route('web.admin.website.index', ['section' => 'media']) }}">Manage media</a></article>
            <article><span>03</span><div><h2>News and enquiries</h2><p>Publish school updates, review contact messages and manage newsletter consent records.</p></div><a href="{{ route('web.admin.website.index', ['section' => 'news']) }}">Manage communication</a></article>
            <article><span>04</span><div><h2>Classic and Dark themes</h2><p>Control semantic colour tokens, preview both themes, publish revisions and roll back safely.</p></div>@if(auth()->user()->hasPermission('website.manage_themes'))<a href="{{ route('web.admin.website.themes.index') }}">Open Theme Manager</a>@else<span>Admin access required</span>@endif</article>
        </section>
    @elseif ($activeSection === 'pages')
        <section class="cms-page-editor-list">
            @foreach ($pages as $slug => $page)
                <details class="action-panel" @if ($loop->first) open @endif>
                    <summary><span><strong>{{ $page->title }}</strong><small>/{{ $slug }} · {{ $page->is_published ? 'Published' : 'Draft' }}</small></span><span aria-hidden="true">+</span></summary>
                    <form method="POST" action="{{ route('web.admin.website.pages.update', $slug) }}" class="long-form cms-page-form">
                        @csrf
                        @method('PUT')
                        <fieldset class="form-section"><legend>Page heading</legend><div class="form-grid form-grid-2"><label class="field-group"><span>Internal/page title</span><input name="title" value="{{ old('title', $page->title) }}" required></label><label class="field-group"><span>Eyebrow</span><input name="eyebrow" value="{{ old('eyebrow', $page->eyebrow) }}"></label><label class="field-group form-span-full"><span>Main headline</span><input name="headline" value="{{ old('headline', $page->headline) }}"></label><label class="field-group form-span-full"><span>Summary</span><textarea name="summary" rows="3">{{ old('summary', $page->summary) }}</textarea></label><label class="field-group form-span-full"><span>Page body</span><textarea name="body" rows="8">{{ old('body', $page->body) }}</textarea></label></div></fieldset>

                        @if ($slug === 'home')
                            <fieldset class="form-section"><legend>Homepage sections</legend><div class="form-grid form-grid-2"><label class="field-group"><span>Welcome heading</span><input name="sections[welcome_title]" value="{{ data_get($page->sections, 'welcome_title') }}"></label><label class="field-group"><span>Admissions heading</span><input name="sections[admission_title]" value="{{ data_get($page->sections, 'admission_title') }}"></label><label class="field-group"><span>Welcome text</span><textarea name="sections[welcome_body]" rows="4">{{ data_get($page->sections, 'welcome_body') }}</textarea></label><label class="field-group"><span>Admissions text</span><textarea name="sections[admission_body]" rows="4">{{ data_get($page->sections, 'admission_body') }}</textarea></label></div><div class="cms-repeatable-grid">@foreach (data_get($page->sections, 'programs', []) as $index => $programme)<div><label class="field-group"><span>Programme {{ $index + 1 }} title</span><input name="sections[programs][{{ $index }}][title]" value="{{ $programme['title'] ?? '' }}"></label><label class="field-group"><span>Description</span><textarea name="sections[programs][{{ $index }}][text]" rows="3">{{ $programme['text'] ?? '' }}</textarea></label></div>@endforeach</div></fieldset>
                        @elseif ($slug === 'about')
                            <fieldset class="form-section"><legend>School values</legend><div class="cms-repeatable-grid">@foreach (data_get($page->sections, 'values', []) as $index => $value)<div><label class="field-group"><span>Value {{ $index + 1 }}</span><input name="sections[values][{{ $index }}][title]" value="{{ $value['title'] ?? '' }}"></label><label class="field-group"><span>Description</span><textarea name="sections[values][{{ $index }}][text]" rows="3">{{ $value['text'] ?? '' }}</textarea></label></div>@endforeach</div></fieldset>
                        @elseif ($slug === 'admissions')
                            <fieldset class="form-section"><legend>Admission pathways</legend><div class="cms-repeatable-grid">@foreach (data_get($page->sections, 'pathways', []) as $index => $pathway)<div><label class="field-group"><span>Pathway {{ $index + 1 }}</span><input name="sections[pathways][{{ $index }}][title]" value="{{ $pathway['title'] ?? '' }}"></label><label class="field-group"><span>Description</span><textarea name="sections[pathways][{{ $index }}][text]" rows="3">{{ $pathway['text'] ?? '' }}</textarea></label></div>@endforeach</div></fieldset>
                        @endif

                        <fieldset class="form-section"><legend>Search and publication</legend><div class="form-grid form-grid-2"><label class="field-group"><span>SEO title</span><input name="seo_title" value="{{ old('seo_title', $page->seo_title) }}"></label><label class="field-group"><span>SEO description</span><textarea name="seo_description" rows="3">{{ old('seo_description', $page->seo_description) }}</textarea></label></div><label class="check-row"><input name="is_published" type="checkbox" value="1" @checked($page->is_published)><span>Publish this page publicly</span></label></fieldset>
                        <div class="form-actions"><button class="primary-button" type="submit">Save {{ str($slug)->headline() }} page</button></div>
                    </form>
                </details>
            @endforeach
        </section>
    @elseif ($activeSection === 'media')
        <div class="split-workspace">
            <section class="workspace-primary"><div class="section-heading compact-heading"><div><p class="eyebrow">Published assets</p><h2>Website media</h2></div><p>Hero, campus and gallery files use protected storage and public model-authorized delivery.</p></div><div class="cms-media-list">@forelse($mediaItems as $media)<article><div class="cms-media-preview">@if($media->media_type === 'image' && $media->publicUrl())<img src="{{ $media->publicUrl() }}" alt="{{ $media->alt_text }}">@else<span>{{ str($media->media_type)->headline() }}</span>@endif</div><div><strong>{{ $media->title ?: 'Untitled media' }}</strong><span>{{ str($media->collection)->headline() }} · Order {{ $media->sort_order }} · {{ $media->is_published ? 'Published' : 'Hidden' }}</span><small>{{ $media->caption }}</small></div><form method="POST" action="{{ route('web.admin.website.media.destroy', $media) }}">@csrf @method('DELETE')<button class="text-button danger-text" type="submit">Remove</button></form></article>@empty<div class="empty-state">No website media has been added.</div>@endforelse</div></section>
            <aside class="workspace-aside"><section class="plain-panel"><div class="section-heading compact-heading"><div><p class="eyebrow">New media</p><h2>Add website asset</h2></div></div><form method="POST" action="{{ route('web.admin.website.media.store') }}" enctype="multipart/form-data" class="form-stack">@csrf<label class="field-group"><span>Collection</span><select name="collection" required><option value="hero">Homepage hero</option><option value="gallery">Gallery</option><option value="campus">Campus media</option></select></label><label class="field-group"><span>Media type</span><select name="media_type" required><option value="image">Image</option><option value="video">Video</option></select></label><label class="field-group"><span>Upload file</span><input name="file" type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/webm"><small>Images up to 10 MB; videos up to 50 MB.</small></label><label class="field-group"><span>Or external media URL</span><input name="external_url" type="url"></label><label class="field-group"><span>Title</span><input name="title"></label><label class="field-group"><span>Alternative text</span><input name="alt_text"><small>Required for meaningful images.</small></label><label class="field-group"><span>Caption</span><textarea name="caption" rows="3"></textarea></label><label class="field-group"><span>Display order</span><input name="sort_order" type="number" min="0" value="0"></label><label class="check-row"><input name="is_published" type="checkbox" value="1" checked><span>Publish immediately</span></label><button class="primary-button" type="submit">Add media</button></form></section></aside>
        </div>
    @elseif ($activeSection === 'news')
        <div class="split-workspace">
            <section class="workspace-primary"><div class="section-heading compact-heading"><div><p class="eyebrow">News directory</p><h2>Announcements and events</h2></div></div><div class="cms-news-list">@forelse($announcements as $announcement)<details><summary><span><strong>{{ $announcement->title }}</strong><small>{{ $announcement->category ?: 'Announcement' }} · {{ $announcement->is_published ? 'Published' : 'Draft' }}</small></span><span aria-hidden="true">+</span></summary><form method="POST" action="{{ route('web.admin.website.news.update', $announcement) }}" class="form-stack">@csrf @method('PUT')<label class="field-group"><span>Title</span><input name="title" value="{{ $announcement->title }}" required></label><label class="field-group"><span>Slug</span><input name="slug" value="{{ $announcement->slug }}" required></label><label class="field-group"><span>Category</span><input name="category" value="{{ $announcement->category }}"></label><label class="field-group"><span>Excerpt</span><textarea name="excerpt" rows="3">{{ $announcement->excerpt }}</textarea></label><label class="field-group"><span>Full content</span><textarea name="body" rows="8">{{ $announcement->body }}</textarea></label><label class="field-group"><span>Publish date/time</span><input name="published_at" type="datetime-local" value="{{ $announcement->published_at?->format('Y-m-d\TH:i') }}"></label><label class="check-row"><input name="is_published" type="checkbox" value="1" @checked($announcement->is_published)><span>Publish publicly</span></label><button class="secondary-button" type="submit">Save changes</button></form><form method="POST" action="{{ route('web.admin.website.news.destroy', $announcement) }}" class="delete-row">@csrf @method('DELETE')<button class="text-button danger-text" type="submit">Delete news item</button></form></details>@empty<div class="empty-state">No news items are available.</div>@endforelse</div></section>
            <aside class="workspace-aside"><section class="plain-panel"><div class="section-heading compact-heading"><div><p class="eyebrow">New update</p><h2>Create news item</h2></div></div><form method="POST" action="{{ route('web.admin.website.news.store') }}" class="form-stack">@csrf<label class="field-group"><span>Title</span><input name="title" required></label><label class="field-group"><span>Optional slug</span><input name="slug"></label><label class="field-group"><span>Category</span><input name="category" placeholder="News, Event, Notice"></label><label class="field-group"><span>Excerpt</span><textarea name="excerpt" rows="3"></textarea></label><label class="field-group"><span>Full content</span><textarea name="body" rows="8"></textarea></label><label class="field-group"><span>Publish date/time</span><input name="published_at" type="datetime-local"></label><label class="check-row"><input name="is_published" type="checkbox" value="1"><span>Publish immediately</span></label><button class="primary-button" type="submit">Create news item</button></form></section></aside>
        </div>
    @elseif ($activeSection === 'testimonials')
        <div class="split-workspace"><section class="workspace-primary"><div class="section-heading compact-heading"><div><p class="eyebrow">Community voices</p><h2>Testimonials</h2></div></div><div class="testimonial-admin-list">@forelse($testimonials as $testimonial)<article>@if($testimonial->photo_path)<img src="{{ $testimonial->photoUrl() }}" alt="{{ $testimonial->name }}">@endif<div><strong>{{ $testimonial->name }}</strong><span>{{ $testimonial->role }} · Order {{ $testimonial->sort_order }} · {{ $testimonial->is_published ? 'Published' : 'Hidden' }}</span><p>“{{ $testimonial->quote }}”</p></div><form method="POST" action="{{ route('web.admin.website.testimonials.destroy', $testimonial) }}">@csrf @method('DELETE')<button class="text-button danger-text" type="submit">Remove</button></form></article>@empty<div class="empty-state">No testimonials have been added.</div>@endforelse</div></section><aside class="workspace-aside"><section class="plain-panel"><div class="section-heading compact-heading"><div><p class="eyebrow">New testimonial</p><h2>Add community voice</h2></div></div><form method="POST" action="{{ route('web.admin.website.testimonials.store') }}" enctype="multipart/form-data" class="form-stack">@csrf<label class="field-group"><span>Name</span><input name="name" required></label><label class="field-group"><span>Role or relationship</span><input name="role" placeholder="Parent, Student, Alumnus"></label><label class="field-group"><span>Quote</span><textarea name="quote" rows="5" required></textarea></label><label class="field-group"><span>Optional photo</span><input name="photo" type="file" accept="image/jpeg,image/png,image/webp"></label><label class="field-group"><span>Display order</span><input name="sort_order" type="number" min="0" value="0"></label><label class="check-row"><input name="is_published" type="checkbox" value="1" checked><span>Publish immediately</span></label><button class="primary-button" type="submit">Add testimonial</button></form></section></aside></div>
    @elseif ($activeSection === 'messages')
        <div class="data-table-wrap"><table class="data-table"><thead><tr><th>Sender</th><th>Contact</th><th>Subject</th><th>Message</th><th>Received</th><th>Status</th></tr></thead><tbody>@forelse($messages as $message)<tr><td><strong>{{ $message->name }}</strong></td><td>{{ $message->email ?: $message->phone }}<small>{{ $message->email && $message->phone ? $message->phone : '' }}</small></td><td>{{ $message->subject ?: 'General enquiry' }}</td><td>{{ str($message->message)->limit(180) }}</td><td>{{ $message->created_at->format('d M Y H:i') }}</td><td><form method="POST" action="{{ route('web.admin.website.messages.update', $message) }}" class="inline-status-form">@csrf @method('PATCH')<select name="status"><option value="new" @selected($message->status === 'new')>New</option><option value="read" @selected($message->status === 'read')>Read</option><option value="responded" @selected($message->status === 'responded')>Responded</option><option value="archived" @selected($message->status === 'archived')>Archived</option></select><button class="text-button" type="submit">Save</button></form></td></tr>@empty<tr><td colspan="6">No public enquiries have been received.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $messages->links() }}</div>
    @elseif ($activeSection === 'newsletter')
        <div class="data-table-wrap"><table class="data-table"><thead><tr><th>Email</th><th>Name</th><th>Consent</th><th>Consented</th><th>Source</th><th><span class="sr-only">Action</span></th></tr></thead><tbody>@forelse($subscribers as $subscriber)<tr><td><strong>{{ $subscriber->email }}</strong></td><td>{{ $subscriber->name ?: '—' }}</td><td><span class="status-badge status-{{ $subscriber->consent && !$subscriber->unsubscribed_at ? 'active' : 'inactive' }}">{{ $subscriber->consent && !$subscriber->unsubscribed_at ? 'Active' : 'Unsubscribed' }}</span></td><td>{{ $subscriber->consented_at?->format('d M Y H:i') }}</td><td>{{ str($subscriber->source)->headline() }}</td><td class="table-actions">@if($subscriber->consent && !$subscriber->unsubscribed_at)<form method="POST" action="{{ route('web.admin.website.newsletter.unsubscribe', $subscriber) }}">@csrf @method('PATCH')<button class="text-button" type="submit">Unsubscribe</button></form>@endif</td></tr>@empty<tr><td colspan="6">No newsletter subscribers are available.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $subscribers->links() }}</div>
    @elseif ($activeSection === 'settings')
        <section class="content-section"><div class="section-heading"><div><p class="eyebrow">Public identity</p><h2>School and contact details</h2></div><p>These values appear across the public header, footer, Contact page and payment/customer integrations.</p></div><form method="POST" action="{{ route('web.admin.website.settings.update') }}" class="long-form">@csrf @method('PUT')<fieldset class="form-section"><legend>School identity</legend><div class="form-grid form-grid-2"><label class="field-group"><span>School name</span><input name="school_name" value="{{ old('school_name', $settings['school_name'] ?? 'Beloved College') }}" required></label><label class="field-group"><span>Short name</span><input name="school_short_name" value="{{ old('school_short_name', $settings['school_short_name'] ?? 'BC') }}"></label><label class="field-group form-span-full"><span>Tagline</span><input name="school_tagline" value="{{ old('school_tagline', $settings['school_tagline'] ?? '') }}"></label><label class="field-group form-span-full"><span>Address</span><textarea name="school_address" rows="3">{{ old('school_address', $settings['school_address'] ?? '') }}</textarea></label></div></fieldset><fieldset class="form-section"><legend>Contact</legend><div class="form-grid form-grid-2"><label class="field-group"><span>Public email</span><input name="school_email" type="email" value="{{ old('school_email', $settings['school_email'] ?? '') }}"></label><label class="field-group"><span>Enquiry-delivery email</span><input name="contact_recipient_email" type="email" value="{{ old('contact_recipient_email', $settings['contact_recipient_email'] ?? '') }}"></label><label class="field-group"><span>Telephone</span><input name="school_phone" value="{{ old('school_phone', $settings['school_phone'] ?? '') }}"></label><label class="field-group"><span>WhatsApp number</span><input name="school_whatsapp" value="{{ old('school_whatsapp', $settings['school_whatsapp'] ?? '') }}"></label></div></fieldset><fieldset class="form-section"><legend>Social and media links</legend><div class="form-grid form-grid-2"><label class="field-group"><span>Facebook URL</span><input name="facebook_url" type="url" value="{{ old('facebook_url', $settings['facebook_url'] ?? '') }}"></label><label class="field-group"><span>Instagram URL</span><input name="instagram_url" type="url" value="{{ old('instagram_url', $settings['instagram_url'] ?? '') }}"></label><label class="field-group"><span>YouTube URL</span><input name="youtube_url" type="url" value="{{ old('youtube_url', $settings['youtube_url'] ?? '') }}"></label><label class="field-group"><span>X URL</span><input name="x_url" type="url" value="{{ old('x_url', $settings['x_url'] ?? '') }}"></label><label class="field-group form-span-full"><span>Campus video URL</span><input name="campus_video_url" type="url" value="{{ old('campus_video_url', $settings['campus_video_url'] ?? '') }}"></label></div></fieldset><fieldset class="form-section"><legend>Homepage behaviour</legend><div class="form-grid form-grid-2"><label class="field-group"><span>Admission button label</span><input name="admissions_cta_label" value="{{ old('admissions_cta_label', $settings['admissions_cta_label'] ?? 'Explore Admissions') }}"></label><label class="field-group"><span>Admission button URL</span><input name="admissions_cta_url" type="url" value="{{ old('admissions_cta_url', $settings['admissions_cta_url'] ?? route('public.admissions')) }}"></label><label class="field-group"><span>Hero slide seconds</span><input name="hero_autoplay_seconds" type="number" min="3" max="30" value="{{ old('hero_autoplay_seconds', $settings['hero_autoplay_seconds'] ?? 7) }}"></label><label class="check-row"><input name="show_live_statistics" type="checkbox" value="1" @checked(filter_var($settings['show_live_statistics'] ?? true, FILTER_VALIDATE_BOOL))><span>Use live student and staff totals</span></label></div></fieldset><div class="form-actions"><button class="primary-button" type="submit">Save public website settings</button></div></form></section>
    @endif
@endsection
